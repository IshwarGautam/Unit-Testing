/**
 * Function to calculate area of rectangle.
 * @param length number
 * @param breadth number
 *
 * @return areaOfRectangle number
 */
export function calculateAreaOfRectangle(length: number, breadth: number) {
  const areaOfRectangele = length * breadth;

  return areaOfRectangele;
}
